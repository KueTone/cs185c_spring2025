To create a file, we can use the touch command

To edit a file we can use the nano text editor

To open a file we could use the open -a TextEdit command

To preview a file, we could use the cat command or head -n 1 for first line or tail -n 1 for last line.